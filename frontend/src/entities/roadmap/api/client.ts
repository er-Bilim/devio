// 'use client';

// import { useQuery } from '@tanstack/react-query';
// import { qk } from '@/shared/api';
// import { getRoadmap, getRoadmaps } from './server';

// export const useRoadmaps = () =>
//   useQuery({ queryKey: qk.roadmaps, queryFn: getRoadmaps });

// export const useRoadmap = (roadmap_id: number) =>
//   useQuery({
//     queryKey: qk.roadmap(roadmap_id.toString()),
//     queryFn: () => getRoadmap(roadmap_id),
//   });
